import { TerminalApplication } from "@/app/components/terminal-application";

export default function ResearchReplayPage() {
  return <TerminalApplication route={{ activeNav: "Research Replay" }} />;
}
